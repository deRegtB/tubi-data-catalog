# Manual overrides — domain/tag/featured assignments by dashboard name (case-insensitive substring).
# Automatic domain inference from Tableau projects + keyword matching handles most dashboards.
# Add entries here only when you need to override the inferred domain.

featured: []
  # - "Dashboard Name Here"

domains:
  General Tubi: []
  Core Experiences: []
  Viewer Growth: []
  Finance & BizOps: []
  Experimentation: []
  Ads: []
  DiscoAI: []
  Infra/Tools: []
  Content: []

tags: {}

# Override pod/team assignment by dashboard name (pod slug keys).
# Use the export button in the catalog to generate these entries.
teams: {}
  # viewer-growth:
  #   - "Dashboard Name"
  # ads:
  #   - "Dashboard Name"

# Override computed status (active/stale) or hide a dashboard entirely.
# hidden → removed from catalog; active/stale → forces that status label.
status_override: {}
  # hidden:
  #   - "Dashboard to remove"
  # active:
  #   - "Stale dashboard that is still maintained"
